syntax = "proto3";

package auth.role.v1;

import "common/audit/v1/audit.proto";

message Role {
  //Name of the role. Is also the primary key
  string name = 1;
  //Audit information for the role record. e.g. who created it and when
  common.audit.v1.Audit audit = 2;
}
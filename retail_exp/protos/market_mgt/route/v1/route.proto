syntax = "proto3";

package market_mgt.route.v1;

import "google/protobuf/timestamp.proto";

// Represents a route
message Route{
  // Id
  string id = 1;
  // Route unique id
  string route_id = 2;
  // Route assigned code
  string route_code = 3;
  // Route assigned name
  string route_name = 4;
  // Sales district id
  string sale_district_id = 5;
  // Route assigned territory code
  string territory_code = 6;
  // Boundary assigned to route
  string boundary = 7;
  // Time this route was created
  google.protobuf.Timestamp created_at = 8;
  // Id of user that updated this route
  string updated_by = 9;
  // Time this route  was updated
  google.protobuf.Timestamp updated_at = 10;
}